{
  "name": "worker",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^6.4.17"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build worker.ts --outdir dist --target bun",
    "dev": "bun worker.ts",
    "start": "bun dist/worker.js"
  },
  "dependencies": {
    "bullmq": "^5.56.8",
    "nodemailer": "^7.0.5"
  }
}
